## 6. Checkpoints

### CP3 — Trigger extension

```sql
CREATE OR REPLACE FUNCTION communication_events_append_only()
RETURNS TRIGGER AS $$
BEGIN
  -- body from c56a
END;
$$ LANGUAGE plpgsql;
```

Apply the function above unchanged.
